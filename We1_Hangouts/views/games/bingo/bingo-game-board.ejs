<div class="bingo-game-container">
    <!-- Game Controls Section -->
    <div class="bingo-controls">
        <div class="board-size-info">
            <h3 id="board-size-display">Board Size: 5×5</h3>
            <div id="current-caller">Current Caller: Waiting...</div>
        </div>
        
        <div class="game-stats" id="game-stats">
            <div class="stat-item">
                <span class="stat-label">Players:</span>
                <span class="stat-value">0</span>
            </div>
            <div class="stat-item">
                <span class="stat-label">Numbers Called:</span>
                <span class="stat-value" id="called-count">0/75</span>
            </div>
        </div>
    </div>

    <!-- Main Game Area -->
    <div class="bingo-main-game">
        <!-- Player's BINGO Board -->
        <div class="bingo-board-section">
            <h3>Your BINGO Board</h3>
            <div id="bingo-board" class="bingo-board bingo-board-medium">
                <!-- Board cells will be generated by JavaScript -->
            </div>
        </div>

        <!-- Called Numbers Display -->
        <div class="called-numbers-section">
            <h3>Called Numbers</h3>
            <div class="last-called">
                <span>Last Called:</span>
                <span id="last-called-number" class="last-called-number">-</span>
            </div>
            <div class="called-numbers-list" id="called-numbers-list">
                <!-- Recent called numbers will appear here -->
            </div>
        </div>
    </div>

    <!-- Caller Section (Only visible to caller) -->
    <div id="caller-section" class="caller-section" style="display: none;">
        <h3>📢 Number Caller</h3>
        
        <!-- Auto-call controls -->
        <div id="auto-call-section" class="auto-call-section">
            <button id="auto-call-toggle" class="btn-auto-call">Start Auto-Call</button>
            <div id="auto-call-status" class="auto-call-status">Manual calling</div>
        </div>
        
        <!-- Manual calling controls -->
        <div class="manual-call-section">
            <button id="call-random-number" class="btn-call-random">Call Random Number</button>
        </div>
        
        <!-- Number selection grid -->
        <div class="number-caller-container">
            <h4>Select Number to Call</h4>
            <div id="number-caller-grid" class="number-caller-grid">
                <!-- Number buttons will be generated by JavaScript -->
            </div>
        </div>
    </div>

    <!-- Players List -->
    <div class="bingo-players-section">
        <h3>Players (<span id="players-count-bingo">0/100</span>)</h3>
        <div id="players-list" class="bingo-players-list">
            <!-- Player cards will be added here -->
        </div>
    </div>
</div>

<!-- Winner Announcement Popup (will be created by JavaScript) -->
<style>
/* BINGO-specific styles */
.bingo-game-container {
    display: grid;
    grid-template-columns: 1fr 300px;
    grid-template-rows: auto 1fr auto;
    gap: 20px;
    height: 100%;
    max-width: 1400px;
    margin: 0 auto;
    padding: 20px;
}

.bingo-controls {
    grid-column: 1 / -1;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: #f5f5f5;
    padding: 15px;
    border-radius: 8px;
}

.board-size-info h3 {
    margin: 0 0 5px 0;
    color: #333;
}

.game-stats {
    display: flex;
    gap: 20px;
}

.stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.stat-label {
    font-size: 12px;
    color: #666;
    margin-bottom: 2px;
}

.stat-value {
    font-size: 16px;
    font-weight: bold;
}

.bingo-main-game {
    display: flex;
    gap: 20px;
}

.bingo-board-section {
    flex: 1;
}

.bingo-board {
    display: grid;
    gap: 2px;
    max-width: 400px;
    margin: 10px 0;
    background: #333;
    padding: 2px;
    border-radius: 8px;
}

.bingo-board-small {
    grid-template-columns: repeat(3, 1fr);
}

.bingo-board-medium {
    grid-template-columns: repeat(5, 1fr);
}

.bingo-board-large {
    grid-template-columns: repeat(7, 1fr);
}

.bingo-cell {
    aspect-ratio: 1;
    background: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.3s ease;
    border-radius: 4px;
    position: relative;
}

.bingo-cell:hover {
    background: #f0f0f0;
}

.bingo-cell.marked {
    background: #4CAF50;
    color: white;
}

.bingo-cell.marked-by-other {
    background: #FF9800;
}

.bingo-cell.free-space {
    background: #2196F3;
    color: white;
}

.bingo-cell.just-called {
    animation: callHighlight 3s ease-out;
}

@keyframes callHighlight {
    0% { 
        background: #FFD700; 
        transform: scale(1.1); 
        box-shadow: 0 0 20px #FFD700; 
    }
    100% { 
        background: inherit; 
        transform: scale(1); 
        box-shadow: none; 
    }
}

.called-numbers-section {
    min-width: 200px;
}

.last-called {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 15px;
    padding: 10px;
    background: #e3f2fd;
    border-radius: 5px;
}

.last-called-number {
    font-size: 24px;
    font-weight: bold;
    color: #1976d2;
    min-width: 40px;
    text-align: center;
}

.last-called-number.highlight {
    animation: numberHighlight 2s ease-out;
}

@keyframes numberHighlight {
    0% { 
        background: #FFD700; 
        color: #000; 
        transform: scale(1.2); 
    }
    100% { 
        background: transparent; 
        color: #1976d2; 
        transform: scale(1); 
    }
}

.called-numbers-list {
    display: flex;
    flex-wrap: wrap;
    gap: 5px;
}

.called-number {
    background: #f5f5f5;
    padding: 5px 8px;
    border-radius: 4px;
    font-size: 14px;
    font-weight: bold;
}

.caller-section {
    grid-column: 1 / -1;
    background: #fff3cd;
    padding: 20px;
    border-radius: 8px;
    border: 2px solid #ffeaa7;
}

.auto-call-section {
    display: flex;
    align-items: center;
    gap: 15px;
    margin-bottom: 15px;
    padding-bottom: 15px;
    border-bottom: 1px solid #ddd;
}

.btn-auto-call {
    background: #28a745;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
}

.btn-auto-call.active {
    background: #dc3545;
}

.btn-auto-call:hover {
    opacity: 0.9;
}

.auto-call-status {
    font-size: 14px;
    color: #666;
}

.auto-call-status.active {
    color: #28a745;
    font-weight: bold;
}

.manual-call-section {
    margin-bottom: 20px;
}

.btn-call-random {
    background: #007bff;
    color: white;
    border: none;
    padding: 12px 25px;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    font-size: 16px;
}

.btn-call-random:hover {
    background: #0056b3;
}

.btn-call-random:disabled {
    background: #ccc;
    cursor: not-allowed;
}

.number-caller-container h4 {
    margin-bottom: 10px;
    color: #333;
}

.number-caller-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(40px, 1fr));
    gap: 5px;
    max-height: 200px;
    overflow-y: auto;
    padding: 10px;
    background: white;
    border-radius: 5px;
    border: 1px solid #ddd;
}

.number-btn {
    aspect-ratio: 1;
    background: #f8f9fa;
    border: 1px solid #dee2e6;
    border-radius: 4px;
    cursor: pointer;
    font-weight: bold;
    transition: all 0.2s ease;
}

.number-btn:hover:not(:disabled) {
    background: #e9ecef;
    transform: scale(1.05);
}

.number-btn:disabled,
.number-btn.called {
    background: #6c757d;
    color: white;
    cursor: not-allowed;
}

.number-btn.just-called {
    animation: callHighlight 3s ease-out;
}

.bingo-players-section {
    grid-row: 2;
    grid-column: 2;
}

.bingo-players-list {
    max-height: 400px;
    overflow-y: auto;
}

.bingo-player-card {
    background: white;
    border: 1px solid #ddd;
    border-radius: 5px;
    padding: 10px;
    margin-bottom: 10px;
}

.bingo-player-card.current-player {
    border-color: #007bff;
    background: #f8f9ff;
}

.player-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 5px;
}

.player-name {
    font-weight: bold;
    color: #333;
}

.winner-badge {
    background: #ffd700;
    color: #000;
    padding: 2px 6px;
    border-radius: 3px;
    font-size: 12px;
    font-weight: bold;
}

.caller-badge {
    background: #17a2b8;
    color: white;
    padding: 2px 6px;
    border-radius: 3px;
    font-size: 12px;
    font-weight: bold;
}

.player-stats {
    font-size: 12px;
    color: #666;
}

/* Winner Popup Styles */
.winner-popup {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2000;
    animation: popupFadeIn 0.5s ease;
}

@keyframes popupFadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

.winner-content {
    background: white;
    padding: 30px;
    border-radius: 10px;
    text-align: center;
    max-width: 400px;
    animation: popupSlideIn 0.5s ease;
}

@keyframes popupSlideIn {
    from { 
        transform: translateY(-50px) scale(0.9); 
        opacity: 0; 
    }
    to { 
        transform: translateY(0) scale(1); 
        opacity: 1; 
    }
}

.winner-content h2 {
    color: #4CAF50;
    margin-bottom: 20px;
    font-size: 2em;
}

.winners-list {
    margin-bottom: 20px;
}

.winner-item {
    padding: 10px;
    margin: 5px 0;
    background: #f8f9fa;
    border-radius: 5px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.win-type {
    background: #007bff;
    color: white;
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 12px;
    text-transform: uppercase;
}

/* Responsive Design */
@media (max-width: 768px) {
    .bingo-game-container {
        grid-template-columns: 1fr;
        grid-template-rows: auto auto 1fr auto;
    }
    
    .bingo-main-game {
        flex-direction: column;
    }
    
    .bingo-players-section {
        grid-column: 1;
        grid-row: 4;
    }
    
    .number-caller-grid {
        grid-template-columns: repeat(auto-fill, minmax(35px, 1fr));
    }
    
    .bingo-board {
        max-width: 300px;
    }
}

@media (max-width: 480px) {
    .bingo-controls {
        flex-direction: column;
        gap: 10px;
        text-align: center;
    }
    
    .game-stats {
        flex-wrap: wrap;
        justify-content: center;
    }
    
    .number-caller-grid {
        grid-template-columns: repeat(auto-fill, minmax(30px, 1fr));
        max-height: 150px;
    }
    
    .bingo-board {
        max-width: 250px;
    }
    
    .bingo-cell {
        font-size: 12px;
    }
}

/* Dark theme support */
@media (prefers-color-scheme: dark) {
    .bingo-controls {
        background: #2d3748;
        color: white;
    }
    
    .bingo-player-card {
        background: #2d3748;
        border-color: #4a5568;
        color: white;
    }
    
    .caller-section {
        background: #744210;
        border-color: #b7791f;
    }
    
    .number-caller-grid {
        background: #2d3748;
        border-color: #4a5568;
    }
    
    .number-btn {
        background: #4a5568;
        border-color: #6b7280;
        color: white;
    }
}
</style>